---
# Install zsh and oh-my-zsh

- name: install zsh
  homebrew:
    name: zsh
    state: latest
  tags:
    - installation

- name: install oh-my-zsh
  shell: '/bin/sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"'
  tags:
    - installation

- name: download powerline fonts for themes
  git:
    repo: https://github.com/powerline/fonts.git
    dest: /tmp/powerline_fonts
    depth: 1
  tags:
    - installation

- name: install powerline fonts
  copy:
    src: /tmp/powerline_fonts/
    dest: /Users/{{ ansible_user }}/Library/fonts/
    directory_mode: 0755
  tags:
    - installation

- name: install themes
  git:
    repo: https://github.com/bhilburn/powerlevel9k.git ~/.oh-my-zsh/custom/themes/powerlevel9k
    dest: /Users/{{ ansible_user }}/.oh-my-zsh/custom/themes/powerlevel9k
  tags:
    - installation

- name: setup zsh config
  template:
    src: zshrc.j2
    dest: /Users/{{ ansible_user }}/.zshrc
  tags:
    - configuration

- name: change shell
  user:
    name: "{{ ansible_user }}"
    shell: /bin/zsh
  tags:
    - configuration
